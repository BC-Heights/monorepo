import { SubTopicCard } from '@the-heights/components';

export const metadata = {
  title: 'Newton Overide Election \u2014 The Heights',
  description: "The Heights'Coverage of the Newton Overide Election.",
};

export default function Page() {
  return (
    <div>
      <SubTopicCard slug="newton-overide-special-election" />
    </div>
  );
}
