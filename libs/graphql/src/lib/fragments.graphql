fragment PostFragment on Post {
    title
    molonguiAuthors {
      displayName
      slug
    }
    id
    date 
    modifiedGmt
    featuredImage {
      node {
        caption
        sourceUrl(size: $imgSize)
        mediaDetails {
          sizes {
            sourceUrl
            name
          }
        }
      }
    }
    excerpt
    slug
    content(format: RENDERED)
    categories {
      nodes {
        name
      }
    }
  }

