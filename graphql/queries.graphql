query getPosts($first: Int!) {
  posts(first: $first) {
    nodes {
      ...PostFragement
    }
  }
}

query getPostIDs($first: Int!) {
  posts(first: $first) {
    nodes {
      id
    }
  }
}

query getPost($id: ID!) {
  post(id: $id) {
    ...PostFragement
  }
}
